[nix]
packages = ["python311", "python311Packages.pip", "chromium", "chromedriver"]

[phases.install]
cmds = ["python3 -m pip install -r requirements.txt"]

[start]
cmd = "streamlit run app.py --server.headless true"
